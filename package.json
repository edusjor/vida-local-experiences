{
  "name": "tours-sys",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:ensure": "prisma migrate deploy && prisma db push --skip-generate",
    "predev": "npm run db:generate && npm run db:ensure",
    "dev": "next dev --webpack -p 3003",
    "prebuild": "npm run db:generate && npm run db:ensure",
    "build": "next build",
    "prestart": "npm run db:generate && npm run db:ensure",
    "start": "next start -p 3003",
    "lint": "eslint",
    "postpull": "npm run build"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "formidable": "^3.5.4",
    "next": "16.1.6",
    "prisma": "^6.19.2",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/formidable": "^3.5.0",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
